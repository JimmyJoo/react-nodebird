// actiion types

// action creator

// initialState
const initialState = {
  mainPosts: [],
};

// reducer
const post = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default post;
